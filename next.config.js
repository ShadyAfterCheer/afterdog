/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "replicate.delivery",
      "pbxt.replicate.delivery",
      "supabase.co",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
